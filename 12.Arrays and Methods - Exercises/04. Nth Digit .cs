using System;

class Test
{
    static void Main()
    {
        int number = int.Parse(Console.ReadLine());
        int toBase = int.Parse(Console.ReadLine());

        while(number > 0)
        {
           int lastIndex = number % toBase;
           int firstIndex = number / toBase;
        }


    }

   
    }

